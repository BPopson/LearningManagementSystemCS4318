<%= form_with(model: assignment, local: true) do |form| %>
  <% if assignment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(assignment.errors.count, "error") %> prohibited this assignment from being saved:</h2>

      <ul>
      <% assignment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :course_id %>
    <%= collection_select(:assignment, :course_id, Course.all, :id, :full_course_name) %>
  </div>

  <div class="field">
    <%= form.label :assignment_name %>
    <%= form.text_field :assignment_name %>
  </div>

  <div class="field">
    <%= form.label :max_grade %>
    <%= form.number_field :max_grade %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
